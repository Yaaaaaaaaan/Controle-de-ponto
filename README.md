I'm so Excited to say us, it is a new environment to control the user access to dependencies from work.

for now, there are:
- User authentication module: functional and refactoring;
- User creation module: fully functional;
- Profile picture change module using photos recently inserted by the user: actually not functional, but refactoring;
- Common user data change module, such as name, email and username:  actually not functional, but refactoring;
- Password change module:  actually not functional, but refactoring;
- Unique presence insertion module, done by the user: mid-functional, it is actually under refactoring (includes: viewing total presence in a summary graph, defined by month, allowing viewing of the 3 most recent months);
- Offline module: partially functional.


Architecture: MVC with API-First.

Technologies used:
- Persistence: Mysql & IndexedDB.
- Languages: PHP, Javascript, HTML5, CSS3.
- Frameworks: Chart.JS, Bootstrap CSS & JS, FontAwesome Icons.
